{
  "name": "reader-mini-app",
  "version": "1.0.0",
  "description": "READER - Your daily reading tracker Farcaster Mini App",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.1.0",
    "@vercel/kv": "^1.0.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
